// Demo of process performance testing
// By: Sam Hocking

import meshtools
import plot
import optimize
import "Particles.morpho"
import "RandomPoints.morpho"
import "Tester.morpho"
import "TimingFuncs.morpho"

var tester = Tester()

// test process time by varying the number of particles
tester.varyNumPoints([5000..20000:5000], 1000, 3)

// test process time by varying the number of mesh vertices
// tester.varyNumVerts([500..2500:500], 2000, 3)

// tester.testLoop(pythonTime, quiet=false, project=true, moveAll=true)
tester.testLoop(morphoClock, quiet=false, projectTimingFlag=true, moveAllTimingFlag=false)
tester.exportTimingDictArr()